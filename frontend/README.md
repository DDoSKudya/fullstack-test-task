# File Exchange Frontend

Vue 3 + Vite dashboard for the file exchange API.

## Scripts

- `npm run dev` — development server on port 3000
- `npm run build` — production build

Set `VITE_API_URL=/api/v1` for production builds. In Docker dev, the Vite proxy forwards `/api` to the API service.
